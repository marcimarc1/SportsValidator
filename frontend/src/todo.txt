Naming Conventions
    file names:
    js files have UppercaseCamelCase names, for class components this is also the name of the class.
    css files corresponding to react class components have the same name as the file/class.

    for CSS classes:
    CamelCase for classes that are used for a certain element. Name them the same as the
    React Component if it's the outer HTML element in that component. If it's inside use "ClassNameElementSubelement..."
    For generic classes use normal lowercase-dashed-case

I use Material UI for some prebuilt components (see https://material-ui.com/getting-started/usage/ )

TODO

    BACKEND
        See pages/trackingEditor/TrackingEditor => state => "//BACKEND" comments for which variables the backend needs
            - get and set all these variables
            - get images + annotations (preferable either pass user authentication token/whatever or "demo" for the demo to work with the backend too)
            - save changes in annotations (positions, names, deleted/added something)
            - get list of all available videos
            - upload new video
            - User Management (sign in, create new user, etc.)

check if/how to add source for fonts/icons etc
So far all icons are svgbox => material
NavBar: Fontawesome used accordingly to https://fontawesome.com/how-to-use/on-the-web/using-with/react
because material ui icons from svgbox does not have double arrow
fonts: Google Fonts

use local version of fonts instead of url in script tag in index.html

change Favicon and Tab Title


QUESTIONS FOR MARC (see answers on paper):
 - is iscrowd used/should I consider it at all?
    just ignore / give option in element "card" to set a track/id to iscrowd?

 - 4 teams?

 - save colors in Backend?
 user should probably be able to change colors (because maybe some do not work on his video because colors are too similar?)
 I can try to save them in cookies, but I think backend makes more sense (cookies can expire/be deleted, access from other pc, etc.)

 - id vs name
 I think Backend should have name for each track and id should not be changeable by user
 One id to name dict per video, not per frame

 - do IDs always start at 0 and are continuous?
 Right now I have a list of bBoxes and search for the one I want, otherwise I could use an Array and position them by ID => faster
 But right now it should be fast enough anyways and more stable...
 Would be better to use a dict of ids! But not sure if that works with the way im passing React Components in a List

 - corners?
